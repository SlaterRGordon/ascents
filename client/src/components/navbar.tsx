import './navbar.css';

interface Props {
	title: string;
}

const Navbar = (props: Props) =>{

    const {
		title
    } = props;

    return (
        <div className='navbar'>

        </div>
    )
};

export default Navbar;